# Laser Nesting Test DXFs

# Laser Nesting Test DXFs

8 nesting test files for laser cutting:

- `nested all.dxf` — the full nest
- `CheckedNestedGroup1..7.dxf` — per-group check nests

Use them to stress-test nesting layouts on the laser bed (all AC1032 DXF).