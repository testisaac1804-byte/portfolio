# IsaacNet

**Category:** Software & Apps · **Status:** Done

Layered bypass. TLS-wrapped, multi-tunnel auto-fallback.

**Stack / Tools:** Networking, Proxy, Python, Bypass

**Build path:**
- V1 — Single TCP tunnel.
- V2 — TLS wrapping.
- V3 — Layered: CONNECT+SOCKS5+WS.

**Location:** `~/projects/`

Layered bypass proxy, TLS-wrapped, multi-tunnel (CONNECT + SOCKS5 + WebSocket) with automatic fallback.
